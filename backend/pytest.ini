[pytest]
# Default run = the offline unit suite. The iteration test modules at the root of
# tests/ talk to a deployed backend over HTTP and need REACT_APP_BACKEND_URL, so
# run them explicitly: `pytest tests/backend_test.py` etc.
testpaths = tests/unit
filterwarnings =
    ignore:on_event is deprecated:DeprecationWarning
