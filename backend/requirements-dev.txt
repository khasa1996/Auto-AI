# Test-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# The offline unit suite (tests/unit) needs only these plus fastapi/pydantic/motor
# from requirements.txt — it stubs emergentintegrations and MongoDB.
pytest>=8.0.0
pytest-cov>=5.0.0
mongomock-motor>=0.0.36
httpx>=0.27.0
requests>=2.31.0
