# Premium frontend contracts

`configuratorPremium.js` contains pure data contracts for configured conversion payloads, purchasable comparison and privacy-safe share-card generation. UI integrations must call these helpers only after the canonical configurator validation succeeds.
